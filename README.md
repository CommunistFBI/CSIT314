# CSIT314
 
# Test  Commit

store the backend files in a folder in your users folder eg C:\Users\yourUser
then using the terminal navigate yourself to the folder having the code files
now type npm install(only need to do this once) in the terminal
then npm start
and the backend should load into local host 5000
